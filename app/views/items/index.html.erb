<p style="color: green"><%= notice %></p>

<h1>Items</h1>
<%= turbo_frame_tag Item.new do %>
<% end %>
<div id="items">
  <% @items.each do |item| %>
  <%= render item%>
  <% end %>
</div>

<%= link_to "New item", new_item_path, data: {turbo_frame: dom_id(Item.new) } %>
